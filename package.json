{
  "name": "mixxx-merge",
  "version": "0.1.0",
  "description": "A command line tool that makes merging Mixxx 2.4 libraries easy.",
  "author": "Magnus Reyes <me@magnusreyes.com>",
  "license": "MIT",
  "type": "module",
  "bin": "src/bin.ts",
  "scripts": {
    "dev": "cargo run",
    "lint": "cargo clippy --fix --allow-staged && cargo fmt",
    "test": "cargo test",
    "build": "cargo build --release",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.0.11"
  }
}
